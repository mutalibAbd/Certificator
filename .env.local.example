# Supabase Configuration
# Get these values from your Supabase project settings: https://supabase.com/dashboard/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=your-supabase-project-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# Supabase Service Role Key (Required for admin operations)
# WARNING: This has full database access. Never expose this to the client.
# Only used server-side in API routes and Server Actions.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Keep-Alive / Cron Secret
# This secret protects the /api/system/pulse endpoint from unauthorized access.
# Used by GitHub Actions to trigger keep-alive pulses every 48 hours.
# Generate a strong random string: openssl rand -base64 32
CRON_SECRET=your-cron-secret

# Also add these secrets to your GitHub repository:
# 1. CRON_SECRET - Same value as above
# 2. VERCEL_PRODUCTION_URL - Your Vercel deployment URL (e.g., https://certificator.vercel.app)

