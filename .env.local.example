# Supabase Configuration
# Get these values from your Supabase project settings: https://app.supabase.com/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=your-supabase-project-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# Keep-Alive Service
# This secret is used by the keep-alive mechanism to prevent Supabase from pausing after 7 days of inactivity
# Generate a strong random string and keep it secure
KEEP_ALIVE_SECRET=your-keep-alive-secret

# Supabase Service Role Key (Optional - only for admin operations)
# WARNING: This has full database access. Never expose this to the client.
# Only use server-side in API routes or server components.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
